<nav class="bg-gray-800" data-controller="navbar">
  <div class="max-w-7xl mx-auto px-2 sm:px-6 lg:px-8">
    <div class="relative flex items-center justify-between h-16">
      <div class="absolute inset-y-0 left-0 flex items-center sm:hidden">
        <button type="button" class="inline-flex items-center justify-center p-2 rounded-md text-gray-400 hover:text-white hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-inset focus:ring-white" aria-controls="mobile-menu" aria-expanded="false" data-navbar-action="toggleNavbar">
          <span class="sr-only">Open main menu</span>

          <svg class="block h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true" data-action="click->navbar#toggleNavbar" data-navbar-target="burger">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
          </svg>

          <svg class="hidden h-6 w-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor" aria-hidden="true" data-action="click->navbar#toggleNavbar" data-navbar-target="cross">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
          </svg>
        </button>
      </div>
      <div class="flex-1 flex items-center justify-center sm:items-stretch sm:justify-start">
        <div class="hidden sm:block sm:ml-6">
          <div class="flex space-x-4">
            <%= link_to "Overview", overview_index_path, class: controller_name == "overview" ? "selected-option" : "option" %>
            <%= link_to "Transactions", transactions_path, class: controller_name == "transactions" ? "selected-option" : "option" %>
            <%= link_to "Tokens", tokens_path, class: controller_name == "tokens" ? "selected-option" : "option" %>
            <%= link_to "Chains", chains_path, class: controller_name == "chains" ? "selected-option" : "option" %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <div class="hidden" id="mobile-menu" data-navbar-target="toggle">
    <div class="px-2 pt-2 pb-3 space-y-1">
            <%= link_to "Overview", overview_index_path, class: controller_name == "overview" ? "selected-option-mb" : "option-mb" %>
            <%= link_to "Transactions", transactions_path, class: controller_name == "transactions" ? "selected-option-mb" : "option-mb" %>
            <%= link_to "Tokens", tokens_path, class: controller_name == "tokens" ? "selected-option-mb" : "option-mb" %>
            <%= link_to "Chains", chains_path, class: controller_name == "chains" ? "selected-option-mb" : "option-mb" %>
    </div>
  </div>
</nav>
